name: Release Notes Placeholder

on:
  workflow_dispatch:

jobs:
  explain:
    runs-on: ubuntu-latest

    steps:
      - name: Explain current flow
        run: |
          echo "This repository is prepared for GitHub-triggered EAS builds."
          echo "Add EXPO_TOKEN in GitHub Secrets before running the Android Preview APK workflow."
